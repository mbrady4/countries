import React from 'react';
import './navbar.scss';

const NavBar = (props) => {
    return (
        <nav>
            <h1>Where in the World?</h1>
            <button>Dark Mode</button>
        </nav>
    )
};

export default NavBar;